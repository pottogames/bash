# pnpm 10 no longer links workspace packages for a plain version range by
# default, and npm cannot parse the `workspace:` protocol at all. Declaring the
# internal dependencies as `*` and turning this on is the one combination both
# package managers resolve to the local package, so `npm install` and
# `pnpm install` both work from a fresh clone.
link-workspace-packages=true
